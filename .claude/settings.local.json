{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -q -m 'Add bucket library picker, custom fallback, and split-into-weeks action *)",
      "Bash(git push *)"
    ]
  }
}
